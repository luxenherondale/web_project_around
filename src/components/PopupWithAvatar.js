// PopupWithAvatar.js - Extiende PopupWithForm para la edición de avatar

import PopupWithForm from "./PopupWithForm.js";

export default class PopupWithAvatar extends PopupWithForm {
  constructor(popupSelector, handleFormSubmit) {
    super(popupSelector, handleFormSubmit);
  }

  // Aplicando con los métodos heredados de PopupWithForm
}
